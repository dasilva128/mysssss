{
  "name": "cloudflare-scanner-panel",
  "version": "5.0.0",
  "private": true,
  "description": "Cloudflare-only scanner panel using Workers Static Assets and D1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply cloudflare-scanner-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
